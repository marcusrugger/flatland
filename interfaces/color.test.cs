using NUnit.Framework;
using System;
using Flatland;


[TestFixture]
class ColorTest
{
/*
    [Test]
    public void ColorRGB()
    {
        Color color = new ColorRGB(1, 2, 3);

        Assert.That(color.Red  , Is.EqualTo(1));
        Assert.That(color.Green, Is.EqualTo(2));
        Assert.That(color.Blue , Is.EqualTo(3));
        Assert.That(color.Alpha, Is.EqualTo(255));
    }

    [Test]
    public void ColorRGB_From_ColorRGB()
    {
        ColorRGB original = new ColorRGB(1, 2, 3);
        Color    color    = new ColorRGB(original);

        Assert.That(color.Red  , Is.EqualTo(   1 ));
        Assert.That(color.Green, Is.EqualTo(   2 ));
        Assert.That(color.Blue , Is.EqualTo(   3 ));
        Assert.That(color.Alpha, Is.EqualTo( 255 ));

        Assert.That(original.Red  , Is.EqualTo(   1 ));
        Assert.That(original.Green, Is.EqualTo(   2 ));
        Assert.That(original.Blue , Is.EqualTo(   3 ));
        Assert.That(original.Alpha, Is.EqualTo( 255 ));
    }

    [Test]
    public void ColorRGB_From_ColorRGBA()
    {
        ColorRGBA original = new ColorRGBA(1, 2, 3, 4);
        Color     color    = new ColorRGB(original);

        Assert.That(color.Red  , Is.EqualTo(   1 ));
        Assert.That(color.Green, Is.EqualTo(   2 ));
        Assert.That(color.Blue , Is.EqualTo(   3 ));
        Assert.That(color.Alpha, Is.EqualTo( 255 ));

        Assert.That(original.Red  , Is.EqualTo( 1 ));
        Assert.That(original.Green, Is.EqualTo( 2 ));
        Assert.That(original.Blue , Is.EqualTo( 3 ));
        Assert.That(original.Alpha, Is.EqualTo( 4 ));
    }

    [Test]
    public void ColorRGBA()
    {
        Color color = new ColorRGBA(1, 2, 3, 4);

        Assert.That(color.Red  , Is.EqualTo( 1 ));
        Assert.That(color.Green, Is.EqualTo( 2 ));
        Assert.That(color.Blue , Is.EqualTo( 3 ));
        Assert.That(color.Alpha, Is.EqualTo( 4 ));
    }

    [Test]
    public void ColorsTest()
    {
        Assert.That(Colors.Red.Red    , Is.EqualTo( 255 ));
        Assert.That(Colors.Red.Green  , Is.EqualTo(   0 ));
        Assert.That(Colors.Red.Blue   , Is.EqualTo(   0 ));
        Assert.That(Colors.Red.Alpha  , Is.EqualTo( 255 ));

        Assert.That(Colors.Green.Red  , Is.EqualTo(   0 ));
        Assert.That(Colors.Green.Green, Is.EqualTo( 255 ));
        Assert.That(Colors.Green.Blue , Is.EqualTo(   0 ));
        Assert.That(Colors.Green.Alpha, Is.EqualTo( 255 ));

        Assert.That(Colors.Blue.Red   , Is.EqualTo(   0 ));
        Assert.That(Colors.Blue.Green , Is.EqualTo(   0 ));
        Assert.That(Colors.Blue.Blue  , Is.EqualTo( 255 ));
        Assert.That(Colors.Blue.Alpha , Is.EqualTo( 255 ));
    }
*/
}
